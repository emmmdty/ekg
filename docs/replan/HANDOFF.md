# v6 方向重审 · 交接状态（2026-08-25）

> 会话因额度中断。本文件记录**重审进行到哪一步、已定什么、下一步做什么**，供后续会话接续。
> ⚠️ 本次重审可能推翻 `docs/SPEC.md` 的 v5 四章设定，**在得出结论前 v5 仍然有效**，不要提前改动主线文档。

## 1. 作者定的新目标（2026-08-25）

重审**全流程方法论**，目标是一篇 3–4 章硕士论文：

| 维度 | 要求 |
|---|---|
| 主题 | 「事件图谱构建及其应用」，**事件图谱 / 事理图谱二选一** |
| 应用场景 | **风险监测**（源自「大宗商品价格风险监测」项目；大宗商品**不是硬约束**，需从中抽子方向） |
| 数据 | 只用公开数据集，**不做人工标注** |
| 算力 | **单卡 5090**（约 27GB 可用；4090 当前不可达） |
| 每章 | 有独立方法贡献，能与已发表方法（经典 + 近两年）**同口径比较并超越** |
| 全篇 | 形成闭环、自洽、逻辑一致 |
| 新颖性 | 允许在他人工作上做优化改进，但要讲清 delta |
| 每章四问 | 动机与背景问题 / 领域进度与卡点 / 我们的方法与创新 / 预期结果 |

**作者补充的边界**：
- 时间线**无约束**，**允许重开项目**，时间来得及。
- 职业落点偏 **LLM/Agent 工程**，也接受图谱/抽取算法方向，**不用硬凑**。
- 语言：**提议**做跨语言（原话「能否做成一个跨语言的」——是**待评估提议，不是已定约束**）。
- 领域：希望选当前热门、方法多、数据集丰富的，最好有**新数据集且其上已有发表方法**。
- 风险监测是**应用场景**，作为方法论的延伸。
- **论文不需要写**，但方案定下来前必须把上述四问想清楚。

## 2. 已修正的方法论（作者确认过的偏差，勿重犯）

本次重审开头我犯了两处错，作者确认「你之前理解的偏差是对的」：

1. **顺序反了**：作者的流程是「先出探索提示词 → 探索 → **依据探索结果**决定」。
   我一份调研没做就抛出「赛道 A/B/C」让作者选 —— 把该由证据定的事变成了拍脑袋。
2. **按「对手好不好打」选题**：我的推荐理由写了「对手最多，最容易兑现超越」
   「垂域对手稀少所以别选」。这是挑软柿子，与「对齐学界，别换指标绕过去」相反。
   **赛道由「问题值不值得做」定，「能不能超越」是随后的工程问题。**

附带：**试探性措辞（能否/可不可以）≠ 已决定的约束**。作者说「能否做成跨语言」，
我直接把它立成全篇主轴并让作者在其形态里挑，这是过度解读。

> 已存入记忆 `ekg-research-before-options`。

## 3. 已完成的产出

| 产物 | 路径 | 状态 |
|---|---|---|
| 探索提示词（7 Block + 5 条硬性原则） | `docs/replan/EXPLORATION_PROMPT.md` | ✅ 完成 |
| 本地资产清点 | `docs/replan/LOCAL_ASSET_INVENTORY.md` | ✅ 完成 |
| 五路探索报告 | `docs/replan/{A..E}_*.md` | ❌ **未落盘** |

**本地清点的关键结论**（详见该文件）：
- **「重开」的代码损失是 2,000–3,000 行，不是 9,962 行。** Tier 1（任务无关，2,263 行）
  含 `core/calibration/` 974 行纯统计原语 + `core/eval/` 536 行标准共指指标，换方向零损耗。
  真正作废的是 `succession/` 2,148 行（强绑 MAVEN+SeDGPL）与 Tier 2 的数据适配层。
- **跨数据集/跨语言 schema 抽象已验证过**：`relations/data/ccks_causal.py` 把中文 CCKS
  金融因果归一化进同一套 schema 跑通。降低换**数据集**风险，**不**降低换**任务**风险。
- **磁盘上有 15 个已归一化数据集**，含 **DocEE 485M（从未用过）**；`data/raw/` 另有
  ECB+/RAMS/WikiEvents/MATRES/SeDGPL-ESC。⚠️ 只确认「文件在」，**split 合规与计数未复核**。
- **`agents/` 176 行且 0 测试**，是唯一无测试覆盖的模块 ⇒ 若走 LLM/Agent 方向基本从零开始。
- 最值钱的是**方法学纪律**（canonical 序 / 配对 bootstrap + 噪声地板 / 强对照 /
  oracle 泄漏红线 / no-op 起步 / 口径三轴对齐），换任何方向都适用。
- 本次实跑确认 `uv run pytest` **exit=0，354 passed / 12 skipped**。

## 4. 中断时的状态

**五路探索子代理已发出（`web-search-agent`），会话中断时报告尚未落盘。**
它们本应写到 `<scratchpad>/exploration/` 再由我收拢 —— **scratchpad 是会话级的，会话一断即失效**。

⇒ **后续会话必须重跑探索**。分工如下（提示词全文在 `EXPLORATION_PROMPT.md`，
每路只需读该文件 + 执行自己的 Block，输出直接写 `docs/replan/`）：

| 代理 | Block | 输出文件 |
|---|---|---|
| A 地形 | Block 1 + 7.1 | `docs/replan/A_terrain.md` |
| B 数据集 | Block 2 + 6 | `docs/replan/B_datasets.md` |
| C 方法与代码 | Block 3 + 5 | `docs/replan/C_methods_code.md` |
| D 两个切口 | Block 4（含自主提新切口） | `docs/replan/D_angles.md` |
| E 工业与市场 | Block 7.2 + 7.3 | `docs/replan/E_industry.md` |

> 重跑时把输出路径直接设为 `docs/replan/`，不要再经 scratchpad 中转。

## 5. 后续流程（未执行）

1. **重跑五路探索**（见上表）。
2. **交叉核对**：查各路数字有无互相矛盾；查有无违反提示词 §1.2（数字不带出处/split/评测脚本）
   —— **违反的部分打回重查，不进综合**。
3. **出综合地形报告 + 决策表**，给出「重构 vs 重开」的结论**连同理由**。
4. 作者认可方向后，出 **2–3 条完整章节骨架**，每条每章带四问，带利弊与明确推荐。
5. 骨架定了才写 spec（`docs/superpowers/specs/`），再做任务规划与实施。

**当前处于 brainstorming 的 architectural 路径**：已完成「探索项目上下文」与部分「澄清提问」，
**尚未进入「提出 2-3 条方案」阶段**。HARD-GATE 仍然有效：**未经作者批准不得写任何实现代码。**

## 6. 未提交的工作区改动（与本次重审无关，属 v5 遗留）

`git status` 显示 Ch1 `PHASE_C3` 步骤 1 的成果尚未提交：
`scripts/report_coref_error_profile.py` + `tests/scripts/test_coref_error_profile.py`
+ `src/ekg/core/eval/` 改动 + `docs/results/PHASE_C.md` 的 107 行结果。
**质量已验证**（自带对官方 evaluate.py 的双向强制交叉校验，354 测试全绿）。
无论重审结论如何，这批**建议先提交**，不要丢。
