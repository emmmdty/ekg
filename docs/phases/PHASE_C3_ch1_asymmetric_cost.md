# PHASE C3 — Ch1 非对称代价的事件身份消解

> **SUPERSEDED 2026-08-27：不得执行。** 错误剖析保留，但非对称方向已被推翻；当前契约见
> [`PHASE_C4_context_identity.md`](PHASE_C4_context_identity.md)。

> 单会话自包含契约。`/clear` 后只读**本文件 + 自动载入的 `CLAUDE.md` + [`../SPEC.md`](../SPEC.md)**
> 即可执行。实测数字见 [`../results/`](../results/README.md)（单一事实源，本文件只引用不复制）。
> 建于 2026-08-07（v5 章节重设后），前身 `PHASE_C` 已归档（见 [`../ARCHIVE_INDEX.md`](../ARCHIVE_INDEX.md)）。

## 0. 先读：三条会让你白干的前提

1. 🛑 **口径只有一个：`77.47`**（官方 `evaluate.py` / 710 篇全量 valid / 无校正）。
   **`79.6` 是内部评测器 + 497 篇子集 + ERE 人群校正的数，不得进任何对外表格。**
   两者三条轴同时不同（评分器 / 文档集 / 校正）。原文见 [`../results/PHASE_C.md`](../results/PHASE_C.md) 第 219 行。
   ⚠️ 2026-08-07 之前 `TODO.md` 的对标行一直混用这两个数，是本项目第三次同类错误。
2. ⛔ **不要再换底座**。longformer −2.5、roberta-large −2.8、大容量档 −2.8 三次全败，
   数字在 `PHASE_C.md`。缺口不在编码器容量，官方基线本身就是 roberta-base 的 81.4。
   （roberta-large 若非要试，须 lr 1e-5，否则发散——但**本 phase 不安排**。）
3. 🛑 **MAQInstruct 的 coref 80.2 不得作为对标线**。指标定义已核清是 MUC，但评测脚本、
   mention 来源、valid 篇数全未声明，且其 79.8 比 dev/test 差外推值高约 3 点 ⇒ 协议大概率不同。
   本 phase 的对标线是**官方 81.4 @test + 显式 dev/test 差声明**。

## Goal（完成目标）

**把 Ch1 共指从 MUC 77.47 抬到超过官方单任务基线 81.4 的口径**，并回答一个具体问题：
**用下游实测的误差代价比来设非对称优化目标，能不能定向修好欠并（recall 缺口）。**

## 依据（⚠️ 2026-08-07 步骤 1 跑完后**已被部分推翻**，先读本节末的更正）

| 事实 | 出处 |
|---|---|
| ~~缺口**全在 MUC recall**（82.56 vs 84.0）~~ **错，见下** | ~~`PHASE_C.md` 官方口径复评节~~ |
| B³/CEAFe 与官方几乎持平（−0.6） | `PHASE_C.md` 官方口径复评节 |
| 下游：**拆节点(欠并) −.0184 = 等幅删边 2.3× = 等幅增边 3.9×** | `PHASE_E.md` 受控扰动曲线 |
| 拆节点是**一致性机器完全看不见**的一类（causal SCC=0、拓扑边数与 gold 相同） | 同上 |

### 🛑 更正（步骤 1 实测，`PHASE_C.md`「欠并/过并错误分布」节）

- **过并 1,391 对（63.5%）＞ 欠并 801 对（36.5%）**，MUC 链同向（452 vs 258）。
- 与官方基线的缺口大头在 **precision −6.22**（72.98 vs 79.2），**不是** recall −1.44（82.56 vs 84.0）。
- 「缺口全在 recall」那句来自**内部口径**（497 篇 + ERE 人群校正，P −0.8 / R −3.2）；
  07-30 换官方评测器后 P/R 排序翻转，结论未跟着重算 —— **同类口径混用第四次**。
- ⛔ **2.3–3.9 是下游单位代价比，不是权重比**：与频率比 0.58×（过并占多数）方向相反，
  净倾斜只剩约 **1.3–2.2×**。
- ⇒ 「训练对称优化 F1」这个观察仍然成立，但**非对称的方向与幅度都要重定**；
  且步骤 1 指出了一个更靠前的抓手：**过并 51.8% / 欠并 44.4% 都是 trigger 完全相同的对**
  ——两个方向在同一批对上同时错，问题不在阈值的对称性，而在词形相同时的判别力。

## 执行内容（Steps）

### 1. 先量化当前的过并/欠并分布（CPU，不训练）— ✅ 2026-08-07 完成

在 710 篇 valid 上按官方评测器拆出：欠并对数、过并对数、各自占错误总量的比例。
**这一步必须先做**——非对称权重的比值要有数据支撑，不能直接套 2.3–3.9（那是下游代价比，
不是错误分布比，两者是两个量）。**做完的结论见上面的更正节，与预期相反。**

复现：`scripts/report_coref_error_profile.py`（命令与全部数字在 `../results/PHASE_C.md`）。
脚本自带两条对官方 `evaluate.py` 的强制交叉校验，不过就 fail，所以表不可能漂离 77.47。

### 2. 非对称损失（单变量）

在现有 `nodes/coref.py` 的成对分类头上，对「该合未合」与「不该合却合」两类错误给不同权重。
⚠️ **zero-init / no-op 起步**——`fin-ekg-m2-structure` 那次的教训：给已调好的编码器加新流，
默认 `N(0,1)` 会碾压既有特征路径致指标腰斩。权重比从 1:1 起扫。

### 3. 非对称聚类阈值（单变量，可与 2 独立）

现有弃权带（band）是对称的。改为对合并方向更宽松、对分裂方向更严格。
**必须在同一难例误合并率上比**——`PHASE_C.md` 已有先例：阈值挪动会冒充增益。

### 4. 联合建模（若 2/3 不够）

官方自己把 81.4→82.1 归因于四关系联合训练（+0.7）。这条与 Ch2 的三族联合是同一个技术动作，
**做之前先看 Ch2 那边的结论**，不要两章并行开两条。

## Constraints

- 遵守 `CLAUDE.md` 全部硬约束。**只增不改**：新机制走命令行参数与 registry，不改默认值
  ——`runs/nodes/coref_supervised_6ep` 是 Ch2/Ch3/Ch4 一系列结果的依赖。
- 报数**一律用官方评测器 + 710 篇全量**。内部口径只许出现在消融的相对比较里，且须标注。
- 每一步都是**单变量**。`window_dist_20ep_officiallr` 那种「同时变四项」的档只能当「求最好结果」，
  不能当消融，归因要另做——别重蹈。

## 验收标准（Done when）

- [x] 步骤 1 的欠并/过并分布表落 `../results/PHASE_C.md`（2026-08-07，官方口径 + 双向交叉校验）。
      **结果与本 phase 的前提相反**，步骤 2/3 开工前须先按上面的更正节重定方向。
- [ ] 非对称损失、非对称阈值各自的**单变量**对照表（官方口径，MUC P/R/F1 + B³ + CEAFe + CoNLL）。
- [ ] **明确回答**：非对称化有没有把 recall 抬起来、代价是多少 precision。
- [ ] 校验命令全绿（`uv run pytest && uv run ruff check src tests scripts && uv run ekg-smoke`）。
- [ ] 数字回填 `../results/PHASE_C.md`，`../TODO.md` 只更新状态与下一步。

## GPU

中（roberta-base 共指训练）。4090 有空即可自用；**5090 须逐次问作者**。开工前 `nvidia-smi` 原子核卡。
checkpoint 训在哪留在哪，**档在哪台必须记进 `../results/PHASE_C.md`**。

## 达不到怎么办（止损）

- 非对称损失 + 非对称阈值两项做完 MUC 仍 <80：说明欠并不是靠**决策规则**能修的，
  转去查**候选生成**（是不是根本没把这些对送进分类器）——`PHASE_C.md` 记过约 1.6 点是结构性的
  （ERE gold coref 对里 1.63% 两端不全在 Arg 人群内）。**不要继续扫权重比。**
- 若查明缺口的大头是结构性的（人群不匹配）：那就**如实写成数据集口径限制**，
  把本章的对标声明改为「在可及人群上达到 X」，并给出人群覆盖率。这比硬凑分数诚实且站得住。
