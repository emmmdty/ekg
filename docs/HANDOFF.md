# 交接文档 · 新窗口从这里开始

> 更新于 **2026-09-18 晚（裁决 ④ 落地：C5 取 (甲) 就此收 · 多种子不授权 · 重心转三章整体提高）**。本文是新窗口唯一必读入口；读完后再按本文链接
> 打开所需文件，不回溯聊天记录。本文只记录**状态、决策、依赖、下一步**，
> **不复制实验表格——数字只认 [`results/`](results/README.md)**。
>
> **状态一句话：三个方法章 0 章过门；论文形态已定稿为「Ch6 承重 + 一章负结果归因」（裁决 ④）。
> 但定稿不等于停工——作者同日指示「不要一直在应用章上动作，前三个方法章要整体提高」。
> ⇒ 当前队首是 `EXPERIMENT_PLAN.md` §10 那份立项分析里的**一个协议裁决**：
> 允不允许动 C5 的负例分布（`--neg-ratio`）。批了就能上 4090 跑，不批就真的收尾。**
>
> **最短读法**：首表 → **§0.3（当前队列）** → 需要哪章再看 §0.4 的对应小节。
> §0.5–§0.7 是已裁定的决策与纪律，开工前扫一眼。
> **文末附录是历史过程记录，新窗口不需要读**，只在要查「当时为什么这么判」时按标题定位。

## ▶ 当前阶段（新窗口先看这张表）

| 项 | 值 |
|---|---|
| **活动任务** | 🟢 **C5 pilot-r3 三臂在跑**（gpu-4090 卡 0/1/2 并行，`setsid nohup`，日志 `logs/c5_r3_{full,remove_core,permutation}.log`，产物 `runs/stages/C5/c5-v61-argument-uncertainty-r1/pilot-r3/`）。作者 2026-09-18 晚批了一格有效周期，单变量＝负例分布。**跑前登记（设置 · 协议边界 · 事前判据）已在 `results/PHASE_C.md` 末节，数字栏留空等回填。** |
| 正式阶段 | Gate 2 实测 **0 个方法章过门**，落 `EXPERIMENT_PLAN.md` §5 的**「≤1 章过」**分支。Gate 1 已判不过（D4，09-12 关闭）。**Ch6 不在这个判定里**，它按 §7.5 独立成立。 |
| ✅ **裁决已给** | 2026-09-18 晚作者批 **C5 一格有效周期**（用在 §10 步 1）、**Ch3 五维瓶颈先不动**（等 C5 结果）。⛔ 仍不得：自行开新机制家族、加大 backbone、换 split、扫 epoch、启动多种子、补训注册对照臂 2（超出本次授权，见 §10.5b）。 |
| **⚠️ 开工前必读** | 每个新任务先答「科研价值 / 可行性」两问（`CLAUDE.md`「开工自审」节）。不可行**必须点名是数据 / 协议 / 代码 / 算力 / 授权哪一条**，附一手证据，**停下交作者裁决**，不得自行换题绕开。 |
| 四章现状（结论与去向在 **§0.4**，数字在 `results/`） | **D4**（Ch3）贡献为零，家族已封存 · **A4**（Ch4）机制输给自己的消融臂且坍塌无差别 · **C5**（Ch5）**唯一臂序正确**但低于主锚 1.08，差距 **100% 在 precision**，H1 接法已证伪并按裁决 ④ 收口 · **E3**（Ch6）主表成形 + 正控通过 + **三对不对齐** + **两条协议发现**，**不再加动作**。🆕 **三章共同的一条**：base head 全部落在自己主锚下方，机制建在这个欠额之上——见 `EXPERIMENT_PLAN.md` §10。 |
| 🔴 **读表 6-2 的前提** | 那张表**不能按 MRR 从高到低读**——评测单元本身有一条 **Hit@10 = 1.000** 的文档捷径，读候选文本的对手吃得到、SeDGPL 族吃不到；另一个对手则因**金标在训练图里 0/1,908 有边**而系统性垫底。两条机制都已量出，**见 §0.4c 末尾**。**不得写「我们超过了 CSProm-KG / SimKGC」。** |
| **⚠️ 唯一权威计划** | **[`EXPERIMENT_PLAN.md`](EXPERIMENT_PLAN.md)** 的 §4 主表与 §5 Gate。本文 §0.3 只是它的当周切片，**不得出现主表以外的新任务**；要偏离顺序**先改主表**。Gate 之间产生的想法进主表 §8 候补区，**不插队**。 |
| 论文结构 | 第3章 D4 事实性 · 第4章 A4 关系 · 第5章 C5 身份 · 第6章 E3 图谱构建与下游应用。原 24 条件 factorial / Holm 家族 / frozen-vs-finetuned 对照**已撤销，不得恢复**。**裁决 ④ 已定稿**：**Ch6 承重 + 一章负结果归因（D4 + A4 + C5）**。⚠️ 定稿是「当前证据下论文长什么样」，不是停工——作者同日要求**三个方法章整体提高**，路径见 `EXPERIMENT_PLAN.md` §10；§10 步 1 若成立，Ch5 有可能回到方法章。 |
| **GPU**（细则 §E.3） | **gpu-4090** 🟢 四卡全空，有空即可自用；⚠️ 隧道反复掉线且作者的 cpolar 脚本**管不了这台**，按三态判活。**gpu-5090** ⚠️ 09-17 晚实测单卡被他人占着，**核卡再用**，且**每次单独取得授权**。**多种子始终另行授权。** |
| ⚠️ **只在 4090 上的大件** | `runs/factuality/predicted_edges_valid.jsonl`（133 MB，**`.1583` 的真上游**）、`predicted_labels_valid.json`、`ch4_sedgpl.pt`（1.5 G）、两个对手的 checkpoint。**本地那份 `runs/relations/supervised_dump.jsonl` 是另一份 dump，别拿它当上游。** |
| 截止与排期 | 实验须在 **2027-02** 前完成；排期与**估算基准率**见 `EXPERIMENT_PLAN.md` §3（按 git 里同类事件的真实起止天数估，不按计算耗时估）。 |
| 完成后必须做什么 | 按 **§6** 五步回填：产物落地 → 写结果页 → 改主表行状态与 commit → 推进队列 → commit + **push**。**没 push 就没交接。** |

> **工作树状态**：`git status` 干净、`HEAD` 等于 `origin/main`——**具体 hash 用 §0.1 的 `git log -3` 自查**，
> 不再往本文里抄（抄了两次，两次都过期）。
> **最新三件套**：**665 passed / 29 skipped**、ruff 0、`ekg-smoke` OK。
> 最新导师可读周报是 [`reports/2026-09-17_周报.md`](reports/2026-09-17_周报.md)
> ——⚠️ 它写于 09-17 **上午**，其第 4 节的「下周计划」**当天下午到 09-18 凌晨已全部完成或作废**，
> 下一份周报从这里接着写，**不要照抄那张计划表**。

## 0. 接手后先做什么

### 0.1 只读检查（三条，全过才开工）

```bash
cd /home/tjk/myProjects/masterProjects/ekg
git status -sb                      # 期望：main 与 origin/main 同步、工作树干净
git log -3 --oneline --decorate
uv run python scripts/audit_r1_consistency.py \
  --output runs/stages/R1/r1-v61-20260904/audit/cross_artifact_audit.json
                                    # 期望：PASS: 36 requirements mapped
```

工作树若出现未说明改动，**先查来源，不覆盖、不清理**。

加一条（新增于 2026-09-18）：**本地三件套全绿不代表能在服务器上跑**——本地没有 torch，
所有 torch 用例都是 `skip`。动到 torch 相关代码，推之前先 ssh 到有卡的机器跑一次对应测试文件。

> **2026-09-18 之后的新窗口**：这些**已全部做完，不必重做**——
> Ch6 第一轮（E3.1 → E3.2 → 表 6-2 → E3.5，记录在 `results/PHASE_E.md` 末尾几节）；
> **裁决 ① 与裁决 ④ 的落地**（主表 §1 / §5 / §7.5 / §8 / **§10** 已写回）；
> **C-11 抑制上界 + C-12 桶内判别力**（记录在 `results/PHASE_C.md`，产物在
> `runs/stages/C5/.../suppression-bound-20260918/`）；
> **§10 那份跨章立项分析**（三章 base head 与主锚的缺口表、Ch4/Ch5 的共因、可行性五条）。
> **当前状态与唯一队首见 §0.3。**

### 0.2 按顺序读这四份（够了，不要重读整个仓库）

1. **[`EXPERIMENT_PLAN.md`](EXPERIMENT_PLAN.md)** —— 唯一权威计划。§4 主表挑任务，§3 看排期与
   5090 边界，§7 看每章最终要交的表长什么样；
2. **[`BASELINE_ROSTER.md`](BASELINE_ROSTER.md)** —— 四章的外部对手名册与 FR-016 保真度路径；
3. 本文 §E.0 的六条交接约束（HEAD 等于 origin/main、单队列、改计划先改文件、不开分支/worktree、
   活动任务独占产物、交接必须已 push）；
4. **`CLAUDE.md` 的「开工自审」节**——每个新任务先答科研价值 / 可行性两问，不可行必须点名
   是数据 / 协议 / 代码 / 算力 / 授权哪一条并交作者裁决。

按需再读：对应章节的 `phases/PHASE_*.md` 契约、`results/PHASE_*.md`（数字唯一权威）。

### 0.3 下一个窗口执行什么：🟢 **等 C5 pilot-r3 收口，然后回填**

作者 2026-09-18 晚给了两条方向性指示，**优先级高于本文原来的队列**：

> 「现在不用做多种子，还没到最终结果。你不要一直在应用章上动作，应用章的分数也一般。
> 前三个方法章要整体提高，整体方案的科研价值更高、科研成果更好。」

⇒ **Ch6 不再加动作**；重心回三个方法章。立项分析在 **`EXPERIMENT_PLAN.md` §10**，**先读那一节**。
作者随后批了 **C5 一格有效周期**（用在 §10 步 1），**Ch3 的五维瓶颈先不动**，等 C5 的结果再说。

#### ▶ 在跑：C5 pilot-r3（三臂并行，gpu-4090 卡 0/1/2）

**§10 的诊断一句话**：三章的 base head 全部落在自己主锚下方
（D4 −.0172 · A4 ≈−1.9 跨机不可相减 · C5 −1.825），**机制是建在这个欠额之上的**；
其中 **Ch4 与 Ch5 的欠额有同一个代码级来源**——训练在重平衡的负例分布上、推理在全量分布上。

**本轮的单变量**＝`include_negative_only_docs`（False → True）。第一个周期把每一篇全 singleton
文档整个丢出训练，而推理要看全部；`build_training_pairs` 的 docstring 自陈这会造成
「a measured train/test prior mismatch **in exactly the false-merge direction**」，
而 G-5b 量出的缺口正是误合并。**开关已在真实数据上验明生效**：进训练的文档由 **1573/2622**
变成 **2511/2622**。`neg_ratio` 与 `hard_fraction` 保持历史默认并一并钉进契约
（第一周期它们是**静默默认值，不在任何契约里**）。

**设置 · 协议边界 · 事前判据全部在跑之前写好**，见 `results/PHASE_C.md` 末节。要点三条：

- 事前判据：`remove_core` 的 MUC precision 是否回到主锚的 `.7884` 量级，`full` 是否 > `80.98472`；
  **不动 ⇒ H2 被否证**，C5 就此定稿进负结果章，**不再申请第三格**；
- 协议边界：契约 `:65` 要求注册对照臂 2 与臂 4–6 的 pair population 逐位一致，而采样旋钮改它
  ⇒ **本周期只判主锚那半条门**，`:104` 后半条如实标「本周期无法评定」。补训臂 2 **超出本次授权**；
- preflight `1a4e857d…4252`（`status=pass`、`code_files=9`、候选 digest 与第一周期同值）。

**接手要做的**：按三态判活查进度（`ps -eo etime` 或日志尾），三臂都落 `status.json` 后
跑 `--aggregate` 汇总，把数字回填 `results/PHASE_C.md` 末节那张空表，然后按 §6 五步收口。
⛔ **不要在回填之前改判据**，也不要因为数字难看去补跑第四臂。

#### ✅ 09-18 完成的四件（数字只在 `results/`，本文不复制）

**0 · 裁决 ④ + §10 立项分析**（本窗口，纯文档）：C5 第二周期取 **(甲) 就此收**；
多种子不授权；重心转三个方法章的整体提高 ⇒ 已写回 `EXPERIMENT_PLAN.md` §1 / §5 / §7.5 /
**§10（新增）** / §8（H2 进候补区）。

**1 · 裁决 ① 落地**（作者取建议 (A)）：**C5 获批第二设计周期** · **Ch6 提为承重章** ·
**D4/A4 合并为一章「负结果与归因」**；C5 二周期不过 ⇒ 定稿 **Ch6 承重 + 一章负结果归因**。
已写回主表 §1 / §5 Gate 2 / §7.5，H1 出 §8 候补区进主表 **C-11**。

**2 · C-11 抑制上界**（本地纯 CPU，未训练未占卡，`results/PHASE_C.md`）：
抑制只能拆簇，故上界不训练即可算。完美抑制 ≥0.8 跨句桶里的 **213 个误合并对**，
`full` 由 `79.90115` 抬到 **`84.64223`**（比主锚 `80.98472` 高 3.658）⇒ **方向不是死路**。
**但桶内 213 错 / 699 对，基率只有 23.36%**；误否决预算曲线给出验收线：
**仅仅追平主锚就要求桶内精度 ≥ 213/513 = 41.5%**（约基率 1.8 倍），且已假设 oracle 召回。
第三条：**这个集中不是机制造成的**——`remove_core` 桶内占比 59.8% ≈ `full` 58.0%，
低的是主锚 51.5%，它是我们这条 coref head 的共同性质。

**3 · C-12 桶内判别力**（本地纯 CPU，`results/PHASE_C.md`）：把**已实现且 `full` 臂已训过**的
角色相容性特征当抑制器用在那 912 个桶内对上，不训练不调阈值——所有操作点桶内精度
**.2330–.2900**，贴着基率 **.2336**，**没有一档跑赢 `full` 自己**（最好 79.56811，−0.333）。
注册中介规则 `incompatible` 触发 **618/912 次**而精度 **低于基率** ⇒ 它标的「角色不相容」
与「是不是误合并」基本无关（同一事件的两次 mention 常有字面不同的填充物）。
⇒ **问题在信号源，不在接法。**

> ⚠️ **别把 C-12 外推成「角色相容性没有价值」**。它的断言只覆盖
> 「≥0.8 相似度 · 跨句 · 已被 `full` 合并」这个桶，以及 `qwen3-full-r3` 那一份论元产物。

**新增脚本**（都纯 CPU、都自带 fail-fast 校验，可直接重跑，命令在结果页）：
`scripts/report_coref_suppression_bound.py`（no-op 行必须逐位复现该臂已登记的 MUC F1 才继续）、
`scripts/estimate_role_suppression_signal.py`。论元预测已从 4090 双端 `sha256` 核对后搬回本地
`runs/stages/C5/.../upstream-arguments/`（`855906d3…142a`，24.7 MB，实测 **38 秒**
——比 `GPU_RUNBOOK.md` 记的 0.4 MB/s 快，那条记载可以更新）。

#### ▶ 顺位 2：可做但非必须（裁决之外唯一能动的）

- `docs/reports/` 下一份周报：从 09-17 那份的第 4 节接着写，**不要照抄它的计划表**
  （那张表当天下午就作废了）。09-18 这三件是它的主要内容；
- ⛔ **matched seeds 13/17/42 已被作者明确否掉**（2026-09-18 晚：「现在不用做多种子，
  还没到最终结果」）。表 6-2 就以**单 seed** 身份写，Ch6 的 Done-when 这一条**如实留缺**，
  不必再申请；
  ⚠️ 顺带查实的一条 bug：`scripts/evaluate_cgep_propagation.py:392–397` 构造 SeDGPL 预测器时
  **没有把 `args.seed` 传进去**，而 `src/ekg/succession/sedgpl.py:198–199` 的 `fit()` 用
  `self.seed`（默认 209）同时驱动 few-shot 采样与 `torch.manual_seed` ⇒ **今天拿 `--seed 17` 去跑，
  会得到与 209 逐位相同的拟合，而 JSON 里照写 `"seed": 17`**。`e3_main_ours.json` 现在记的 209
  只是两侧默认值刚好都是 209 才碰对的。**修它是一行，但只有在多种子有去处时才有意义，故未动**；
- **MCPredictor / BART contrastive 维持 (b) 不跑**，障碍原文在名册 §6.2b。
  三个公开对手已满足名册 §5 的「不少于 3 个」，**不必再投**。

#### ⛔ 一律不做（不变）

D4 第二个周期、调 threshold、扫 epoch、换 split、加大 backbone、用 seed 17/42 去捞 D4、
把探测数字写进主表或与 33.17 / 32.10 相减、为了让 C5.3 跑通去放宽那条 fail-fast、
自行降级论文结构、开新机制家族、**为了让对手好看去换映射或调它的超参**。

### 0.4 四章现状（数字唯一权威在 `results/`，这里只给结论与去向）

三个方法章的第一个设计周期**全部结束**，没有一个在跑；**三者的失败性质不同，不要合并成一句话。**
Ch6 的第一轮已收口，是四章里唯一有正结果的。

#### 0.4a Ch4（A4 pair evidence）：机制输给自己的消融臂，且坍塌是**无差别**的

**契约跑（A4.3，09-17 收口，gpu-5090，契约 `11e4343a…490c`，seed 13，`final_valid_accessed=false`）**：

| 臂 | causal F1 | 相对同机 `remove_core` |
|---|---:|---:|
| `remove_core`（无机制参照） | **31.224848** | — |
| `no_constraint` | 10.23 | −20.99 |
| **`full`（机制）** | **7.507508** | **−23.72** |
| `length_matched` | 4.38 | −26.84 |

三条判定线 33.17 / 32.097314 / 32.009563 **全部未过**；subevent 22.911283 **破护栏 28.75**；
temporal 52.457177 过。**四臂阶梯与设计意图完全相反。**
⚠️ 与那三条判定线的比较**跨机器**（它们训在 4090 backbone 上，G-13 实测偏移约 1 个 causal F1 点）
⇒ **四臂互比干净，与判定线只陈述限制、绝不相减**。

**归因已做完（G-4b，09-17，离线）**：**坍塌是无差别的，不是「先丢难例」**。逐对 diff——
`full` 丢掉 1,344 对 = `remove_core` 答对集合的 **88.8%**，这批的触发词间距分布与 `remove_core`
答对全体、与 gold 全体**几乎逐格相同**；`full` 答对的 **84.5%** 是 `remove_core` 也答对的。
损失集中在 **PRECONDITION**（占 gold 76.8%，召回 26.81% → 2.12%，只保留 7.9%；CAUSE 保留 23.2%）。
⇒ **排除**「只在难例上失效」与「学到有偏好的表示」两个解释。

**更早的一层归因（09-15 三臂探测，仍成立）**：subevent 整族塌陷的根因是那个 `retained` detach，
**不是 (戊)**；detach 已回滚（`5cfbeff`），代码里留了注释——没有隔离测量之前不得再 detach 一次。
真正压住 causal recall 的是**证据流本身**那条在残缺输入上训练 encoder 的 revised CE，
对它的两种处理各有代价（留着 ⇒ recall 塌；detach ⇒ subevent 塌）。

**状态**：等**裁决 ①**（§0.3）。第二设计周期**未获批准**，不得启动。

#### 0.4b Ch5（C5 argument uncertainty）：**臂序第一次是对的**，但整条 head 低于主锚

**契约跑（C5.3 pilot-r2，09-16，gpu-5090，契约 `7a56e451…b6b0`，`code_files=9`）**：
291 篇 / 8,914 mention 全覆盖，重复 0、陌生 0。

| 臂 | MUC F1 | vs 主锚 80.98472 | vs 注册对照 80.36759 | BLANC F1 |
|---|---:|---:|---:|---:|
| **full** | **79.90115** | **−1.084** | −0.466 | 90.2893 |
| remove_core | 79.15966 | −1.825 | −1.208 | 89.6895 |
| permutation（负控） | 78.83333 | −2.151 | −1.534 | 89.5839 |

**full > 消融 > 负控，MUC 与 BLANC 同向——7 个机制里第一个臂序正确的。**
但 `gate.above_anchor=false`、`above_registered_control=false` ⇒ **门未过**。

**归因已做完（G-5b，09-17，离线）**：`remove_core` 与主锚之间那 1.825，
**误合并解释 72.7%、漏合并 26.4%**。更要紧的一条：**`full` 的 MUC recall `0.846422`
已经超过主锚的 `0.832461`，它与主锚剩下的 1.084 是 100% 的 precision 问题**（0.756630 vs 0.788430）。
⇒ 由此提出 H1：第二周期该把角色相容性接到**抑制侧**，不是继续加 recall。
🔴 **H1 已于 09-18 被 C-11 + C-12 证伪**，见下。

⚠️ **+0.742 仍不是已确证的效应**——本 split / 本指标的噪声地板**至今没量过**
（D4 的 ±.01 是 macro-F1 的地板，不能搬）。⛔ 不跑 seed 17/42（未授权，且两项 gate 皆 false）。

**H1 的两步离线检验（C-11 + C-12，09-18，本地纯 CPU，均未训练未占卡；数字在 `results/PHASE_C.md`）**：

| | 结论 | 关键数 |
|---|---|---|
| **C-11 上界** | 抑制侧**有空间** | 完美抑制 ≥0.8 跨句桶的 213 个误合并对 ⇒ `full` `79.90115` → **`84.64223`**（主锚 +3.658） |
| **C-11 代价** | 但桶**不是信号** | 桶内 213 错 / 699 对，基率 **23.36%**；验收线＝桶内精度 **≥41.5%** 才追平主锚 |
| **C-11 归属** | 集中**不是机制造成的** | `remove_core` 59.8% ≈ `full` 58.0%，低的是主锚 51.5% |
| **C-12 信号** | 现有特征**填不满** | 所有操作点精度 .2330–.2900（基率 .2336），**没有一档跑赢 `full` 自己** |
| **C-12 中介** | 注册中介在此**失效** | `incompatible` 触发 618/912，精度 **低于基率** |

⇒ **问题在信号源，不在接法**；换信号源＝新机制家族，须回 R1 立项。

**状态**：**裁决 ④ 取 (甲)**——H1 这条接法就此收，Ch5 并入「负结果与归因」章。
⚠️ **但 C5 并没有走完**：`EXPERIMENT_PLAN.md` §10 指出那 1.825 的**base head 欠额**与 Ch4 同源
（训练重平衡负例 / 推理全量），而机制自己的 `+0.742` 是实测已登记的
⇒ base 若达到锚就越过锚。**这条等 §0.3 的协议裁决**，批了是一个 2–4 小时的 4090 任务。
⛔ 裁决之前不启动训练、不申请 GPU。

#### 0.4c Ch6（E3 / G-11a）：**第一轮已收口，四章里唯一有正结果的**

| 项 | 值 |
|---|---|
| evaluation unit | **已冻结**：`runs/stages/E3/e3-v61-20260917/`，1,908 实例 / 437 篇有实例（候选池覆盖 607 篇）/ 6,892 候选节点，`queries.jsonl` `f75e7e87…a11e6`。⚠️ 旧的 `e3-v61-20260913` 有 **68 个 `instance_id` 撞号**，09-17 修根因后重新冻结——**逐行零差异**、`candidate_id_digest` 不变，与 `.1802/.1583` 并表的能力没丢。**新对手一律跑新 unit** |
| E3.1 | ✅ 三层上游接口闭合，`upstream_registry.json` `status=closed`。裁决 ② 取 (乙)：`predicted` 上游 = **v5 判别式抽取器在 valid 上的产物**，不是 C5/A4/D4（那三章与 unit 文档集交集实测为 0）。**表头必须标明这个身份** |
| E3.2 | ✅ 描述性画像：predicted 拓扑边是 gold 的 **3.4 倍**、R2 query F1 **.0795**，而下游 MRR 只掉 **12.2%** |
| E3.3 表 6-2 | ✅ **三个公开对手到齐**（SeDGPL / CSProm-KG / SimKGC），加 random / frequency / **`same_document`** 三个平凡对照。⚠️ **单 seed**。**读表前提见本节末尾的两条，先读再引数字** |
| E3.4 | ✅ 已完成且本轮直接引用：构建损失 −.0218 ＝ 整图价值的 **22.0%**；拆节点 −.0184 > 删边 −.0081 > 增边 −.0047；打乱 temporal 恒 0（**结构零**） |
| E3.5 | ✅ 已写：三条结论各自的「可写 / 必须同时写 / 不可写」，Done-when 逐条核对 |
| 三个对手的保真度 | 全部 **(b) 透明适配**——**没有一个实现 CGEP**，SeDGPL 作者的适配从未发布，差异逐条在 `export_manifest.json`。CSProm-KG 另在 **WN18RR 上取得 (a)**（MRR 0.572682 vs 公布 0.572660），那证明的是「我们把它跑对了」，**不改变它在本协议上的 (b)** |
| MCPredictor / BART contrastive | **不跑**，维持 (b) + 障碍原文（名册 §6.2b）。三个对手已满足名册 §5 的「不少于 3 个」 |
| ⛔ 唯一缺的 | **matched seeds 13/17/42**——须逐次授权，不得自行启动 |

**本章带出的两条不依赖方法章的发现**（写进论文的，不只是实验记录）：

1. **三对不对齐**：causal_scc↔R1 ρ=−0.064（而 R1↔R2 ρ=+0.783）、**R2↔MRR**、**协议↔能力**
   ⇒ 「图谱一致、可重建、下游有用」是三件不同的事，而且彼此不对齐；
2. **协议发现**：本地重建的 CGEP-MAVEN 里负采样跨文档而 ECG 不跨文档，叠加出一条
   Hit@10 = 1.000 的捷径。**原论文的派生数据未发布，我们无法判断它是否也有这条**——
   只能如实说「我们的重建协议有，而且量出来了，并给出了不受影响的比较轴」。

🔴 **读表 6-2 的两条前提**（机制都已量出，不是推断。**不读这两条就别引那张表的数字**）：

1. **评测单元本身有一条捷径**：候选是**全语料**采的而 ECG **从不跨文档** ⇒ 金标永远与 anchor
   同篇，而 512 个候选里平均只有 2.1 个同篇。`same_document` 对照因此拿到 **Hit@10 = 1.000**。
   **读候选文本的方法吃得到，按 mention token id 打分的（SeDGPL ⇒ 本文两行）吃不到。**
   SimKGC 清空 description 后塌 11.3 倍，top-1 同篇率 78.20% → 18.50%（均匀乱选 0.41%）
   ⇒ **它带候选句那档的九成分数是文档匹配**。**主行取无 description 档**，那才与 SeDGPL 族同轴；
2. **CSProm-KG 朝相反方向失败**：按**实体嵌入表**打分，而金标在训练图里 **0/1,908** 有边
   （CGEP 要求尾节点 outdeg 0 / indeg 1，任何 triple 级切分都得拿掉那条边），Hit@50 恒 0。
   **内部对照**（dev `val_mrr=0.2907`，仍在全部 33,017 实体里排）证明模型没坏；
   **也不是训练不足**——未见实体只收负梯度，多训只会拉大差距。

⇒ **论文里不得写「我们超过了 CSProm-KG / SimKGC」**。能写的是：本文构建图在这批 query 上
高于两个公开 KGC 对手与两个平凡对照，**而两个对手的落后各有已量出的结构性解释**。
**三条限制必须同写**：单 seed；SimKGC batch 被显存压到 256；CSProm-KG epoch 取 60。

### 0.5 已裁定的决策（**都已执行完毕，不要重新讨论**）

详细推导与当时的一手证据在**已移出的附录**（见文末）；这里只留「决定是什么」和「还留下什么约束」。

| 日期 | 决策 | 现在还剩什么约束 |
|---|---|---|
| 2026-09-15 | **允许使用 5090**；4090 以后再补，不要因为它拖慢进度；**优先下载而不是跨机搬运**；网络有问题时用镜像 | 仍适用。跨机搬运单程约 70 分钟（实测带宽约 0.4 MB/s），值不值得由作者定 |
| 2026-09-15 | A4 recall 塌陷取 **(戊)**：`sufficiency` 改为与 `necessity` 同作用面 | 已落码（`2d125cd`），**保留**——三族全部持平或上升 |
| 2026-09-15 | Ch6 **先做 CSProm-KG**；SimKGC 落 (b) | ✅ 已完成，(a) 已取得 |
| 2026-09-15 | LLMERE 保真度记 **(b) Unverifiable**，不碰封存的 final-valid | ✅ 已写进名册 §2.1。⚠️ **这与 C-3 是两件事**——C-3 管的是我们自跑的那一轮 |
| 2026-09-15 | SeDGPL 的 ESC **不再为 (a) 投入**，落 (b) 并写清 .0599 vs .196 的差距 | ✅ 已写进名册 §6.2 |
| 2026-09-16 | **A4 也搬 5090**（「4090 不可行就使用 5090」） | 代价已如实标注：判定线训在 4090 backbone，**四臂互比干净，与判定线不相减** |
| 2026-09-16 | A4 第一个周期取 **(甲)**：维持「戊 in / A out」，四臂按契约跑完，如实交 Gate 2 | ✅ 已执行完毕。**变更窗口已关闭**：不接受事后改成 (乙)/(丙)，也不得以「探测分数不好看」提前宣布失败 |
| 2026-09-17 | **裁决 ②：Ch6 上游身份取 (乙)** —— `predicted` 沿用 v5 判别式抽取器在 valid 上的产物 | ✅ E3.1 已据此闭合。**约束**：表头必须标明上游身份 = v5 抽取器，**不是** C5/A4/D4 |
| 2026-09-17 | **裁决 ③：C-3 / G-8 维持不执行** —— LLMERE 重生成虽已降到 < 1 GPU·h，但它是 Ch4 主表的一行 | ✅ 仍适用，直到裁决 ① 定了 Ch4 存废 |
| 2026-09-17 | **裁决 ①：改纲推迟到 Ch6 主表有数字之后** | ✅ **条件 09-18 满足并已裁定**，见下一行 |
| **2026-09-18** | **裁决 ①（作者取 (A)）**：**Ch6 提为承重章** · **D4/A4 合并为一章「负结果与归因」** · **C5 获批第二设计周期**（限 H1 这一条接法，同一机制家族） | 已写回主表 §1 / §5 / §7.5。⚠️ **H1 当天被 C-11 + C-12 证伪** ⇒ **Ch5 存废回到一个新裁决**（§0.3）；兜底形态仍是裁决 ① 写好的「Ch6 承重 + 一章负结果归因」。**未授权**：新机制家族、多种子、调门槛护栏 |
| **2026-09-18 晚** | **裁决 ④（作者）**：C5 第二周期取 **(甲) 就此收** ⇒ **论文形态定稿「Ch6 承重 + 一章负结果归因」**；⛔ **G-9 多种子不授权**；⛔ **停止在应用章加动作**，**重心转三个方法章的整体提高** | 已写回主表 §1 / §5 / §7.5，立项分析在主表 **§10**。⚠️ 定稿≠停工：§10 步 1 若把 C5 的 base 抬到锚，已实测的 `+0.742` 会越过锚 ⇒ Ch5 有可能回到「过门」。**等 §10 的协议裁决** |
| 2026-09-17 | **cgep `instance_id` 改用 query edge 自己命名**，unit 重新冻结为 `e3-v61-20260917` | 逐行零差异、candidate digest 不变。**新对手一律跑新 unit**；旧 `e3-v61-20260913` 只作历史 |

**两条从这些裁决里沉淀下来、以后都适用的原则**：

1. **phase 的成败必须由契约那一跑判定**，不能拿探测数字给 phase 判死刑（D4 的 Gate 1 就是这么判的）；
2. **有证据的 (b) 比拿不到的 (a) 更结实**——C-4b 已在原文声明的口径下实测到 3 倍差距，
   再投一次只是「重复确认一次失败」。
3. **「待裁决」要分两类**（2026-09-17 作者指示「不需要我决策，你自行推进」之后沉淀的）：
   **可行性断了**（点名数据 / 协议 / 代码 / 算力 / 授权哪一条 + 一手证据）⇒ **仍然停下问**；
   **只是选项题、推荐已写好** ⇒ **按推荐落地、写回主表与结果页、继续干**，不要回头再问一遍。
   裁决 ②③ 就是这样落地的；裁决 ① 因为改的是论文是什么，仍然留给作者。

### 0.6 跑完四章后沉淀下来的，带进任何新机制

1. **先查契约点名的入口脚本在不在**。D4 的 `run_d4_typed_cue_oof.py` 从没被写过，
   却在契约的 GPU 命令里躺了很久（C-9 才补上）。A4 的洞在 C-6b 补上，C5 的在 C-5b 补上——
   **三章各栽过一次**。新 phase 开工第一件事：核对契约点名的入口脚本存不存在。
2. **pilot 入口必须进 preflight 的 `code` 哈希集合**，否则那一跑是唯一钉不住的代码。
   D4 为此重建了 preflight（`preflight-r2`，`code_files=7`，旧的不覆盖）。
3. **报增益前先看噪声地板**。5090 的独立重建把 D4 anchor 的可复现地板量到约 **±.01**
   （见 `results/PHASE_D.md`）；小于这个量级的差不要当效应。
4. **每个新 unit 都要有一个「不读任何东西就能拿多少分」的对照**（2026-09-18 学到的）。
   `random` / `frequency` 说明任务不平凡，但它们说不出**任务在哪个方向上恰恰是平凡的**。
   Ch6 的 `same_document` 只看文档归属就拿到 **Hit@10 = 1.000**——而这条捷径正好落在
   文本类对手站的位置上。**新 phase 冻结评测单元时，顺手想一想「有没有一个单比特能解掉它」。**
5. **外部对手的数字先问「它读了什么」，再问「它多强」**。同一个 checkpoint，
   给不给候选描述，差 11.3 倍。

#### 环境既成事实（不用再查）

- ⚠️ **「没有外网」这句话太粗，2026-09-15 按域名逐个实测后改成这张表**（gpu-5090）：

  | 目标 | 结果 | 影响 |
  |---|---|---|
  | `gh-proxy.com` 镜像上的**增量 `git fetch`** | ✅ | ekg 仓库本轮就是这样同步的；**远端代码不必走 scp** |
  | 同一镜像上**完整 `git clone`** | ❌ `fetch-pack: unexpected disconnect`（`--depth 200` 也断） | 外部仓库改为**本地 clone → tar → scp** |
  | `pypi.tuna.tsinghua.edu.cn` | ✅ **HTTP 200** | **服务器能装包**：CSProm-KG 升 torch、C-2b 的 EasyECR 活体 venv 都因此可行 |
  | `github.com` / `huggingface.co` 直连 | ❌ | 与 09-14 记录一致 |
  | `drive.google.com` | ❌ | 公开 checkpoint 只能本地下载后 scp |

  ⚠️ **2026-09-17 又测出一条，同一个教训第二次**（gpu-4090）：

  | 目标 | 结果 | 影响 |
  |---|---|---|
  | **`hf-mirror.com`**（HuggingFace 镜像） | ✅ **200，且实测能拉权重**（`bert-large-uncased/model.safetensors` 直接下完） | **推翻「公开 checkpoint 只能本地下载后 scp」**——那条结论来自测 `huggingface.co` 与 `drive.google.com`，**没人测过镜像** |
  | `aliendao.cn` | ✅ 200 | 备选 |
  | `modelscope.cn` | ✅ 302 | 备选 |
  | `pypi.tuna.tsinghua.edu.cn` | ✅ | 4090 同样能装包 |
  | `github.com` / `huggingface.co` | ❌ | 与 5090 一致 |

  ⇒ **预训练权重优先在服务器上从 `hf-mirror.com` 直接拉**，别再默认 70 分钟的 scp。
  仓库代码仍走本地 clone → tar → scp（gh-proxy 的完整 clone 会断）。

  **带走的纪律**：粗粒度的「没外网」把**能装包**和**能拉 checkpoint** 混成了一件事，
  直接影响了两项任务的可行性判定。按域名测，别按感觉写。
  ⇒ **外部仓库、预训练权重、公开 checkpoint 一律本地下载后 `scp`**，没有第二条路。
  实测带宽约 **0.4 MB/s**（50 MB > 2 分钟）——`CLAUDE.md` 那句「单程约 70 分钟」就是这么来的。
  这条影响**每一个**外部对手复现（CSProm-KG 的公开 checkpoint、BART contrastive 的 NEEG 数据同理），
  排期时先把搬运算进去。**不要再在服务器上试 `git clone` 或 `from_pretrained` 拉模型。**
- 三个 gitignored JSON 已双端核对（`protocol.json` `f0b4702b…50829`、`t024_freeze.json` `9133a73c…587e7`、
  `cross_artifact_audit.json` `622d094b…f8467`；4090 旧档备份 `protocol.json.pre-e12-20260911`）；
- ⚠️ **2026-09-15 新增：cpolar 有两种不同的失效，别用同一套动作去修。**
  作者的 `~/.local/bin/cpolar-ssh-update` 只能治第一种：

  | 症状 | 更新脚本的输出 | 该做什么 |
  |---|---|---|
  | **端口变了**（常态） | `matched: tcp://…` 然后改写 `HostName`/`Port` | 跑一次脚本即可 |
  | **通道整个下线** | `ERROR [gpu-5090] no tunnel named 'ssh' and none with local='127.0.0.1:22'` | **脚本无能为力**——面板上那条隧道不在了。只能等它回来，**期间按三态判活，不得判定远端进程已死** |

  2026-09-15 12:5x 就是第二种：`Connection refused` + 上面那条 ERROR，同一时刻 `gpu-a6000` 的隧道正常。
  **当时 5090 上正跑着 A4 探测（`setsid nohup`、PPID=1、独立 session），按记录它不受本机断连影响。**
  一次 1.69 GB 的 rsync 在 34 分钟后被这次断连打断（`Broken pipe`）——**所以大文件一律带 `--partial`**，
  回来续传而不是重头再来。

- ⚠️ **gpu-5090 / gpu-a6000 的 cpolar 端口每天都会变，这是常态，不是故障**。作者自己的
  `~/.local/bin/cpolar-ssh-update` 由 systemd user timer（`cpolar-ssh-update.timer`，每日 00:00 +
  开机后）登录 cpolar 面板抓新端口，**只重写 `~/.ssh/config` 里对应 Host 块的 `HostName` 与 `Port`
  两行**，块内其他行原样保留。**不要把端口变化当异常去排查，也不要去改那个脚本。**
- 2026-09-12 已一劳永逸修掉由此引起的 `Host key verification failed`：给 `Host gpu-5090` 加了
  **`HostKeyAlias gpu-5090`**（`gpu-a6000` 块本来就有），host key 改按固定别名查找，端口再换也不影响，
  **且校验一点没放弃**。别名下钉的就是作者确认过的那把 ED25519
  `SHA256:Jkfb9Tb14Z/SqsG6g9GedDjKZOcBl1DLW6zT0V1dkJY`，实测可连。
  该行在 Host 块内且不是 `HostName`/`Port`，**不会被每日脚本覆盖**；改动前的 config 备份为
  `~/.ssh/config.before-hostkeyalias-20260912`。
  今后**只有指纹本身与上面这串不一致时才算异常**——那意味着隧道后面换了机器，**停下问作者，不得自行 TOFU**；
- ⚠️ **gpu-5090 上的 `runs/stages/R1/r1-v61-20260904/protocol.json` 是 09-04 18:30 的旧档**
  （`67a36354…c1c8`，`phase_contracts` 是**空的**），所以 `tests/scripts/test_prepare_d4_typed_cue_preflight.py`
  在 5090 上必然 FAIL（`R1 protocol has no factuality phase contract`）。**这不是代码问题**，是
  那三个 gitignored JSON 只被要求同步到 4090、5090 从没同步过。2026-09-12 用 5090 跑全量
  pytest 时实测到这一条；其余用例全过。要在 5090 上跑任何 D4/A4 preflight，先同步这三个 JSON
  并双端核 SHA-256（本地值见 `results/PHASE_R1.md` §21.3）——**同步动作本身请作者点头**，
  因为它会成为那台机上的信任根。
- 两台机的 cpolar 隧道都会掉线。**ssh 失败 ≠ 远端进程死亡**，三态判活；长任务一律
  `setsid nohup` + `python -u` + 重定向 `logs/`，**一条 ssh 只发一个后台任务**。
  实测这样起的进程 PPID=1、独立 session，本机关机不影响。

### 0.7 纪律清单（每条都是花代价买来的，开工前扫一遍）

1. **一次只变一处。** 09-14 把发散归给 B（同跑还带着 A）是错的；09-15 的探测同时带 A 与 (戊)，
   于是 subevent 塌陷**归不了因**，只能再花 5 小时跑第三臂。**重哈希 probe 契约时数一下
   「几个文件变了」——只该有 1 个。**
2. **判据写在跑之前，跑完不许挑。** (戊) 的判据事前写的是「4,000 量级＝成立 / <1,000＝无效」，
   实测 1,711 **两个分支都不落**，就如实记为「都不落」，不往任何一边圆。
   CSProm-KG 的 (a) 容差同理，**已在跑之前登记**。
3. **ssh 失败 ≠ 远端进程死亡。** 09-15 12:5x 隧道整个下线时，5090 上的探测照常跑完
   （`setsid nohup`、PPID=1）。三态 ALIVE / GONE / ssh 失败，**只有成功 ssh 读到进程 GONE 才算结束**。
4. **冒烟没覆盖的路径等于没测。** C5.3 死在 `build_maven_ere_submission.py` 上，而冒烟从不跑它。
   新机制的冒烟要覆盖**正式 pilot 会走的每一个子进程**。
5. **粗粒度的环境判断会把可行的任务判成不可行。** 「两台机都没外网」这句话把**能装包**和
   **能拉 checkpoint** 混成一件事；按域名逐个测之后（§0.6 的表），C-2b 与 CSProm-KG 的可行性都变了。
6. **改了被契约钉住的文件，就必须重建 preflight**，旧的不覆盖。D4 的 `preflight-r2`、
   C5 的 `preflight-r2`（`7a56e451…b6b0`，`code_files=9`）、A4 5090 线的 `11e4343a…490c`
   都是这么来的——**三章各重建过一次**。
   ⚠️ **反过来也要记住（C-3/E3.1 学到的）**：pilot 一旦收口，就**再也补不进新字段**——
   改 runner 会让契约失效，而各臂钉的是旧 `contract_sha256`，`aggregate` 会拒绝。
   **要加的字段必须在上卡之前加。顺序决定可行性。**
7. **大文件传输一律 `--partial`。** 一次 1.69 GB 的 rsync 在 34 分钟处被隧道打断；带了 `--partial`
   就是续传，不带就是重来。
8. **报增益前先看噪声地板**（D4 anchor 实测 ±.01），小于它的差不要当效应。
9. ⚠️ **推理配置要逐项对着训练配置过一遍——这一条两个月内栽了两次。** C5.3 首跑：推理侧拿不到
   训练时绑定的论元产物（提交脚本根本没有 `--argument-predictions`）。LLMERE：推理侧没有继承训练
   target 的终止语义，于是 **11,149 条全部退化重复**（输出中位 1,755 字符 vs target 98），
   而它**看起来是跑完了的**——95 条解析失败只是污染落进第一行的那一小撮。
   **"它跑完了"不等于"口径对上了"**：新 baseline 上卡前，把训练配置与推理配置两张表并排逐项核。
10. ⚠️ **管道会吞掉退出码。** `rsync … | tail -3` 让一次被隧道打断的传输（`Broken pipe`，
   只落了 101/291 行）照报 `exit code 0`。**传完先核大小或行数再用**，别信退出码。
11. ⚠️ **数字不合常理就先查口径，别先写表。** SimKGC 第一次打出 `.6253`（gold 档的 3.5 倍），
   查下去发现是评测单元的文档捷径。**本项目在这个形状上已经栽过五次**（Phase C 白跑两轮、
   Phase A 判错达标、Ch1 79.6 vs 77.47、C3 方向搞反、A4 拿被证伪的假设当依据），
   这是第六次遇到、**第一次在写表之前拦住**。
12. ⚠️ **ssh 重试封装只能套只读 / 幂等命令。** 隧道常在远端**已经执行完之后**才断，
   重试会把同一条命令再跑一遍——本轮就这样让一个补丁脚本跑了两次，第二次报「已应用」，
   把人引去查一个不存在的 anchor 问题。
13. ⚠️ **补丁的「已应用」哨兵必须是只有它自己才会引入的文本。** 用共享 marker、
   或用上一处改动写进去的字符串当哨兵，都会让后一处改动被误判成已应用。
   **同一个形状的错，本轮隔着一个文件犯了两次。**
14. ⚠️ **改被装饰器修饰的函数，anchor 要包含装饰器行。** 把代码插进 `@torch.no_grad()`
   和它的 `def` 之间是语法错误，而 python **只在 import 时才报**——本轮那次是在
   50 epoch 训练跑完之后才暴露的。
15. ⚠️ **对手代码里「已发表数字没跑过的路径」在新版依赖上大概率是坏的。**
   CSProm-KG 的 (a) 是纯推理，于是 `-n_lar` 训练路径第一个 step 就炸
   （新 torch 在调自定义 `distance_function` 前先校验 `positive.ndim`）。
   **先跑一次小步冒烟，别直接开正式训练。**

## 1. 当前裁决与状态

### 研究目标

课题仍是 occurrence-level 事件图谱。**章号以首表为准**：第3章 D4 事实性 · 第4章 A4 关系 ·
第5章 C5 身份 · 第6章 E3 图谱构建与下游应用（作者 2026-09-11 选定）。
⚠️ 本文 §2 与部分附录仍用**旧章号**（Ch1 身份 / Ch2 关系 / Ch3 事实性 / Ch4 下游），各处已标注，
**读到旧章号时先对照这里换算**。

论文目标原本是**三个高质量方法章 + 一个系统评估章，不降标**。
⚠️ **2026-09-17 起这句话处于待裁决状态**：Gate 2 实测 0 章过门，落「≤1 章过」分支，
最终形态由作者裁决（§0.3 裁决 ①）。**在裁决下来之前，执行代理既不得自行降标、
也不得把「不降标」当作可以自行启动第二设计周期的依据。**

旧机制失败不等于取消方法章：同一机制家族两个有效周期未过门就封存；新家族必须重新通过文献、
协议、因果链、功效和新颖性审查。执行代理不得自行改成两方法章。

### SDD 文档边界

| 层 | 权威文件 | 只负责 |
|---|---|---|
| 研究治理 | [constitution](../.specify/memory/constitution.md) | 不可妥协的有效性、可追溯性和修订原则 |
| 稳定需求 | [`SPEC.md`](SPEC.md) | WHAT/WHY、研究场景、质量底线、可验收结果 |
| 可迭代方案 | [`RESEARCH_PLAN.md`](RESEARCH_PLAN.md) | 候选方法、评测设计和当前依赖计划 |
| 可执行任务 | [`TASKS.md`](TASKS.md)、[`phases/`](phases/README.md) | 步骤、输入、门、停止条件和产物 |
| 运行状态 | [`TODO.md`](TODO.md)、本文 | 当前做到哪里、下一动作 |
| 实验事实 | [`results/`](results/README.md) | 数字、口径、产物和真实 PASS/FAIL |

SPEC **没有**固定 C5/A4/D4 的具体实现、baseline 名单、GPU、epoch 或执行顺序。当前候选中心假设是
evidence adequacy / uncertainty propagation；R1 若找到反证或更强设计，应改 plan，不应把候选方法硬塞回
SPEC。只有研究问题、范围或质量标准改变时才修订 SPEC。

### Git 与验证

- 协议代码整改：`23b4fad fix(protocol): centralize P1 manifest and hashing logic`；
- SDD/方法论整改：`c870ed7 docs(research): separate stable spec from adaptive methodology`；
- R1 审计代码/结果：`8e3eb7a feat(r1): add dataset and prospective power audits`、
  `4e893c1 docs(r1): record method design gate evidence`、`f1401bc feat(r1): freeze factuality
  cross-validation folds`、`32cfd46 feat(r1): close relation handoff and power gate`、
  `277b36f fix(r1): isolate factuality OOF training source`；
- 本文提交后以新的 `origin/main` HEAD 为准；
- **最新三件套：655 passed / 29 skipped、ruff 0、`ekg-smoke` OK**（2026-09-18，Ch6 那一轮收工时）。
  前值 639/29（09-16，`a1e1509`）；
- ✅ **09-16 那处可追溯缺口已补**：`a65f456` / `791a03a` 之后的三件套已在 09-17 晚全量重跑；
- ⚠️ **本地三件套全绿不代表能在服务器上跑**：本地没有 torch，所有 torch 用例都是 `skip`。
  **任何 torch 相关改动，推之前先 ssh 到 5090 跑一次对应的测试文件**（`.venv/bin/python -m pytest <file>`）；
- local gate 的 `tested_tree_sha256`：
  `52c639ff79fb9401f71d42d4d1ecaec6abacf65701cecf72f1dcac7edb04f46e`（220 个文件；
  前值 `3bff2ac2…c701d3c` 对应 `886185e`）；
- A3.6 只运行了已授权 seed 13；R1 功效只读 train-derived internal-dev。跨数据 ID 审计按合同读取了
  public-valid 结构字段但未计算关系/事实性指标，访问已在 ledger 披露；没有搬运 checkpoint。

## 2. 已经成立和不得改写的事实

只从下列结果文档取精确数字。⚠️ **这张表用的是旧章号**（Ch1 身份 / Ch2 关系 / Ch3 事实性 /
Ch4 应用），与现行章序（**第3章 D4 事实性 · 第4章 A4 关系 · 第5章 C5 身份 · 第6章 E3 应用**）
不是同一套编号；**按结果页认，不按章号认**。


| 主题 | 权威入口 | 当前结论 |
|---|---|---|
| Ch2 工作点与检索 | [`results/PHASE_A.md`](results/PHASE_A.md) | 工作点两个核心周期已用完；近似 retriever 三条均未过门；prototype/ATLoss 已封存 |
| Ch1 历史方法 | [`results/PHASE_C.md`](results/PHASE_C.md) | 旧方法未稳定胜出；event-level gold argument 只能作泄漏型 oracle |
| Ch3 历史方法 | [`results/PHASE_D.md`](results/PHASE_D.md) | 旧 D3：与强 baseline 未统计分开。**新 D4：typed-cue 家族 2026-09-12 失败并关闭**（Gate 1「不过」分支），消融与负控都赢过 full |
| Ch6（旧称 Ch4）图谱构建与下游应用 | [`results/PHASE_E.md`](results/PHASE_E.md) | 图依赖正控通过、构建损失 = 整图价值 22.0%；**2026-09-18 第一轮收口**：表 6-2 三个公开对手到齐（全 (b)）、三对不对齐、一条协议捷径发现。~~同实例 factorial~~ **已撤销，不得恢复** |
| P1 可信根 | [`results/PHASE_P1.md`](results/PHASE_P1.md) | r15 是 A3.6 与当前 R1 的可信根；旧 A3 结果继续绑定各自历史根 |

A3.6 **官方训练配方分账已经完成**。它只分解 rates、coref auxiliary、per-family checkpoint selection
三个复现变量，不是论文创新。最高 causal F1 仍未越过冻结主锚，A3 以 `a3-v6-20260905-r17`（protocol
`c187bf03…9359e`）不可变 `failed` bundle 关闭；之后只由 R1 决定新的 Ch2 方法家族。

禁止：第三个工作点、第四个近似 retriever、继续 prototype/ATLoss、把 r13 已观察到的 by-family 曲线
回收成第 4 臂、用更大 backbone 或换 split 救旧机制。

## 3. 后续任务：按依赖执行

当前依赖图：

```text
P1 r15 ─→ A3 failed handoff r17 ─┐
R1 准备任务 ─────────────────────┴→ R1 baseline/input closure
                                                           ├→ C5 候选方法
                                                           ├→ A4 候选方法 ─→ E3 → H2
                                                           └→ D4 候选方法
```

C5/A4/D4 之间没有被 SPEC 固定顺序。R1 未发现真实数据/模型依赖时，可以重排或占不同 GPU 并行；E3
必须等待三类所需上游 handoff。多种子始终需作者再次明确授权。

⚠️ **上图那条「E3 等三类上游 handoff」的箭头实测接不上，已于 2026-09-17 改道。**
三个方法章的产物与 E3 evaluation unit **文档集交集为 0**（unit 在 maven_ere valid 的 437 篇，
C5/A4 在 train 切出的 291 篇，D4 在 maven_fact train 的 2,913 篇），
三份已登记的 fallback 全是「另一个 split 上的预测文件」。
⇒ **裁决 ② 已取 (乙)**：`predicted` 上游改为 v5 判别式抽取器在 valid 上的产物，**表头标明身份**。
**依赖图上 C5/A4/D4 → E3 这条边已断开**，Ch6 不再等任何方法章（§7.5 本就这么写）。

### 任务 E：两个执行代理的交接约束与 GPU 规则（2026-09-07 起，**仍适用**）

#### E.0 统一约束：Claude 与 Codex 交替推进，不并行、不分叉

两个执行代理**轮流**持有同一条队列，任何时刻只有一个"活动任务"。约束只有六条，违反其一即视为交接失败：

1. **单队列**：任务顺序只认 **[`EXPERIMENT_PLAN.md`](EXPERIMENT_PLAN.md) §4 的主表**，本文 §0.3
   是它的当周切片。要偏离顺序，**先改主表再执行**，不在会话里口头改计划。
   （~~E.1 / E.2 / E.2a / E.2b 的旧队列~~ 已被主表取代，正文见文末指向的移出件。）
2. **开工前对齐**：除 §0 的三条只读检查外，必须确认 `git rev-parse HEAD` 等于 `git rev-parse origin/main`；
   不相等就先 `git pull --rebase`，未对齐不开工。
3. **交接靠文件不靠记忆**：一项任务算完成，必须四件齐全——产物落地、数字写进对应
   `results/PHASE_*.md`、本表该行状态改为 `done` 并填 commit、**已 push 到 `origin/main`**。
   没 push 就没交接，下一个代理不得开工。
4. **不开分支、不开 worktree**：只在 `main` 上按逻辑单元提交。（2026-09-07 已删除
   `.worktrees/r1-t023-t024` 与已并入 main 的 `feat/r1-t023-t024`。）
5. **活动任务独占产物**：持有活动任务的代理独占该任务涉及的全部结果页与 `runs/` 目录；
   另一代理此时**只读**，不写任何 `results/`、`runs/`、`TASKS.md`。
   *唯一例外（2026-09-07 作者裁决）*：E8 的长训练只写 `baselines/relation/llmere-causal-*` 这一个
   namespace，持有队列的代理**可以先把它挂到后台再继续做文档行**；回填结果页时才需要重新独占。
   *第二例外（2026-09-09 作者明确授权）*：E8 generation 期间可推进不接触其 namespace 的 D4.0–D4.2。
   D4 在实现、本地 gate、immutable preflight 和 CPU/CUDA smoke 全部通过前不得启动 seed-13 pilot；其 pilot
   独占自己的 `runs/stages/D4/`，不得与 E8 共用 GPU0，也不得启动额外 seeds。
6. **未完成就交接**：把已做到哪一步、卡在什么证据上写进本表该行，状态标 `wip`，不留只有自己知道的上下文。

#### E.1a 作者裁决（2026-09-07）：relation baseline 门重新界定，LLMERE 缩小并行

E3 把 QR-001 的名单问题交给作者后，作者当日裁决**采纳 (b) 的缩小并行版本**。要点：

1. **改门，不改章**：近期 MAVEN-ERE 方法**无一发布可跑的官方训练代码**（RESIJ / 2025 two-stage / KnowQA /
   TacoERE / LLMERE 全部缺），所以「第二个**官方实现**」这道门投入再多 GPU 也过不去——是我们自己的运行口径
   写错了。改回 SPEC `QR-001` 本身的措辞：**第二个不同方法族 · 我们在冻结协议下跑通 · fidelity 缺口写明 ·
   且不弱于主锚**。`SPEC.md` 不修订。
2. **不接受稻草人**：`taco-s13-r3` 只有 causal 32.01 < 主锚 33.17，赢它由赢主锚蕴含。只接受
   「主锚 + 透明适配」被否决。
3. **LLMERE 缩小成 causal-only**：48,365 训练 / 11,149 推理，约 joint 的 1/4；subevent/temporal 是**我们方法的
   护栏**而非 baseline 的义务（`official_single` 是既有先例）。**B2 不需要 Meta 许可**——用未设门镜像 + 披露
   权重 SHA-256。
4. **并行，不串行**：A4 pilot 只吃一张卡而 4090 有四张；第二 baseline 只在确认性 promotion 时生效，
   所以 **T024 照常冻结 A4 契约**，LLMERE-causal 以"已规格化、数字 pending"入 roster。
5. **接受的风险**：LLMERE 若高于主锚，Ch2 及格线抬高、A4 可能失败——**这正是现在跑它的理由**。
   ⚠️ **后续（2026-09-17）**：A4 已经失败了，但**不是**输给 LLMERE——LLMERE 那一轮自己也没产出可评分的
   数字（生成侧缺终止语义，11,149 条全部退化重复，见 `results/PHASE_R1.md` §23）。这条风险条款
   **没有被兑现过**，两件事各自独立失败。
6. **明确不做**：拿 LLMERE 已发布的预测在 710 篇 official valid 上用我们的评测器重打分（那是封存的
   final-valid，不值这笔账）。

完整裁决与规格见 [`results/PHASE_R1.md` §13](results/PHASE_R1.md)；brief 的 roster/promotion 已按此修订并在
`amendments[0]` 保留原文（修订发生在任何 A4 数字存在之前）。

#### E.1b 作者裁决（2026-09-07）：Ch1 门同样重新界定，IP&M 口径核实前置到 E6 之前

E4 把两个问题交给作者后，作者当日给出两条裁决。完整依据见
[`results/PHASE_R1.md` §15](results/PHASE_R1.md)。

1. **Ch1 也改门，理由与 §E.1a 同构**：MAVEN-ERE 文档内共指上除主锚外**没有第二个公开可跑的方法**
   （IP&M 2024 无代码 · OmniEvent EAE checkpoint 失效 · TextEE 无 checkpoint · RESIJ 未取得 ·
   CorefPrompt 正依赖那个失效 checkpoint）。E.1a 给 Ch2 加「不弱于主锚」是为了挡稻草人，对 Ch2 有意义
   （LLMERE 真可能强）；对 Ch1 不成立——唯一有公开仓库的 ACCI 是跨文档方法，`ECB+ 88.4` 不保证过
   `.809847`，**大概率低于主锚**，维持原措辞会让移植的期望价值接近零。新措辞是四条同时成立：
   **独立发表工作的机制 · 在其原始基准上有强证据 · 我们在冻结协议下跑通 · 缺口逐表写明**。
   `SPEC.md` 不修订；`taco` 式自造弱变体仍被第 1 条挡住。
2. **顺序倒过来：先静态核查，再谈训练**（队列 E10）。理由是 E2 刚教过——**仓库有方法本体不等于有
   trainer**（LLMERE 全历史 5 个 commit 无训练代码）。核查成本近零却直接决定这条路通不通。
3. **`qwen3-argument-s13-r2` 角色重定义**：它低于主锚不是废数据，而是「**朴素把预测论元池化进编码器
   会掉点**」的注册负面对照，正是 C5 立论的直接证据。**不再充当第二方法族。**
4. **IP&M 2024 口径核实前置到 E6 之前**（队列 E9）：该文同时报 **causal 37.4** 高于 Ch2 主锚 33.17，
   而 E6 就要冻结 A4 契约——**先核实再冻结，别用一个可能已被超越的及格线冻结整章**。
   判据用 MAQInstruct 那把现成的尺子：**看它复现的 official joint baseline 与我们自跑官方码是否对齐**。
   三种处置（可比 / 口径不同 / 拿不到全文）见 §15.3。无论结果如何**它都不能进 roster**——无公开代码，
   按 FR-006 无法在我们协议下跑通。

#### E.3 GPU 使用（2026-09-16 重写，旧边界已被两次裁决取代）

卡是公用资源，**不得为了「看起来在跑」启动无准入的训练**；但**卡空着也不要串行**
（约束分级的 C 类，见 `CLAUDE.md`）。

| 机器 | 现行规则 |
|---|---|
| **gpu-5090** | 作者 2026-09-11 第二次裁决 + 2026-09-15 追认：**≤1 天的任务直接执行，不必逐次请示**；超过一天、拉模型、跨机搬运仍须先问。C5 整条、A4 全部探测都在这台机上。⚠️ **2026-09-17 晚实测它的单卡被他人占着**（18.3/32.6 GB，91% util）——**核卡再用**。 |
| **gpu-4090** | 有空即可自用（本地三件套全绿后无需逐次点头）；**不挤占他人正在跑的卡**。🟢 **2026-09-18 凌晨四卡全空**（Ch6 的两个对手已跑完）。⚠️ **隧道反复掉线**（`banner exchange` / `kex_exchange` reset），作者的 `cpolar-ssh-update` **管不了这台**（不在其 tunnels.conf 里，它在另一个 cpolar 主机上）——只能等，期间按三态判活；长任务 `setsid nohup` 起的 PPID=1，断连不影响。~~A4.3 必须在这台机上~~ 已被作者 2026-09-16 裁定推翻。 |
| 两台通用 | 选卡前 `nvidia-smi` 核卡；长任务 `setsid nohup` + `python -u` + 重定向 `logs/`，**一条 ssh 只发一个后台任务**；checkpoint 训在哪留在哪；**多种子始终另行授权**。 |

#### E.3a 「先验能先验的」方针（作者 2026-09-13 定，**仍适用**）

> 原话："你不要老是说 4090 不行，4090 不行，就去 5090 上去，把后面的探测性方法先搞好，
> 确保我们后续在 4090 上跑就是有效的。"

**把「等某张卡」换成「把能先验的先验完」。** 判断一件事该不该先在另一台机上做，只问一句：
**它能不能减少那一次正式跑白跑的概率？**

| **该**先做 | **不该**先做 |
|---|---|
| 端到端 dry-run：入口脚本、产物清单、中介统计、aggregate 断言 | 拿探测分数去调设计、挑参数、改阈值——**那才是污染** |
| 实现地板诊断（G-13 那类：消融臂能不能训到该到的量级） | 把探测数字写进主表，或与契约 backbone 的数字相减 |
| 依赖关系与数据可用性核查、外部对手的环境与 import 冒烟 | 用探测结果替代正式 pilot，或据此宣布过门 / 未过门 |

**已经这样做过并有效的三次**：G-13（实现地板）、A4 四臂 dry-run、
**2026-09-17 Ch6 两个对手的小步冒烟**（CSProm-KG 的 1-epoch 冒烟证明了管道通，
也正是它让 `-n_lar` 那条从没跑过的训练路径在 6 分钟而不是 6 小时之后暴露）。

⛔ **已被推翻、不要再照着执行的旧条款**（原文留在附录）：「主体实验仍回 4090」「长任务 pilot 一律
不放 5090」「5090 每次使用须逐次授权」「Qwen3-8B 装不下 5090」——最后一条在 2026-09-13 实测作废
（那个约 17 GB 的服务早已不在，卡上只有 197–209 MiB 常驻）。

⛔ **仍然成立的硬边界**：EasyECR 跑不了 5090（`torch==2.0.1` 不支持 sm_120）；
SimKGC 的 `--batch-size 1024` 要 4×32 GB，两台机都装不下——**2026-09-17 已从推断升为本机实测**：单张 4090 上 1024 与 512 都 OOM、256 稳定（19.9 GB），见名册 §6.2b。

**Ch1 的 ACCI GPU 需求已取消**：E10 判定其公开仓库 `not_runnable`；不得从论文散文重建后称作透明移植。

## 4. R1 后的候选方向：不是固定答案

以下只用于指导 R1 反证与对照设计，不能绕过 R1 直接实现：

- **Ch1/C5**：mention-local predicted argument-role posterior + uncertainty-gated clustering；gold event-level
  arguments 仅作 oracle；
- **Ch2/A4**：完整候选全集上的 pair evidence + sufficiency/abstention risk；retrieval/hard negatives 只能
  改变训练/evidence，不能裁评测候选；
- **Ch3/D4**：typed cue spans + known/unknown → modality → polarity；五类 macro-F1 仍是主门；
- ~~**Ch4/E3**：三类真实上游的同实例 factorial~~ ⚠️ **已撤销**（2026-09-11 改为带公开对手的应用章，
  24 条件 factorial / Holm 家族 / frozen-vs-fine-tuned 对照**不得恢复**）。现行形态见
  `phases/PHASE_E3_graph_application.md` 与 §0.4c。

R1 若证明上述候选与近期工作重复、数据不支持、功效不足或中介不可识别，应提出实质不同且更简单的
候选，并在 `RESEARCH_PLAN.md` 版本化记录。不要为了维护 v6.1 名称而维护错误方法。

## 5. 永久红线与服务器规则

- final-valid 不参与结构、超参、threshold、epoch、stop 或 claim 选择；历史访问继续披露；
- manifest、候选全集、evaluator、输入前提、TIMEX 开关必须成对一致；
- Ch1–Ch3 必须在统一公开主指标上超过主锚和另一强方法族，并通过 matched seeds、document-cluster
  paired CI、guardrails、核心消融和负控；辅助指标不能替代；
- oracle 明确标 non-deployable；缺 ID、未知端点、schema/hash 漂移立即 fail-fast；
- 数字下降、零结果、SSH/工具失败均如实记录；
- 本地禁止 GPU；**4090 有空即可自用**（`CLAUDE.md` 2026-07-17 授权，本地三件套全绿后无需逐次点头，
  选卡前 `nvidia-smi` 核卡、不挤占他人）；**5090 每次单独取得授权**；**多种子始终另行授权**；
- 服务器只用 `.venv/bin/python`，不得 `uv run`/`uv sync`；
- 不运行 `rsync --delete`、远端 `git clean -fdx`，不宽目录递归删除；
- checkpoint 训在哪留在哪；跨机搬运先问作者并做双端 SHA-256；
- 一条 SSH 只启动一个后台任务；长任务使用 `setsid nohup`、`python -u` 和独立日志；
- 改代码后必须运行 `uv run pytest`、`uv run ruff check src tests scripts`、`uv run ekg-smoke`。

## 6. 完成、回填与交接程序

一项任务走完下面五步才算完成；缺任何一步，下一个窗口都不得开工。

1. **产物落地**：命令、日志与 artifact 落到 `runs/stages/<PHASE>/<run root>/`；跨机产物双端 SHA-256 一致。
2. **写结果页**：数字只写进对应的 `results/PHASE_*.md`，升降如实、失败如实；其他文档只引用不复制。
3. **回填队列**：把 `EXPERIMENT_PLAN.md` §4 主表里该行状态改掉并填 commit，同步 §0.3 与顶部
   当前阶段表；没做完写清卡在哪条证据上，不留只有自己知道的上下文。
4. **推进队列**：确认下一行前置已满足。**整条队列清空时**，由本次持有者按
   `EXPERIMENT_PLAN.md` §4 主表与 §5 的 Gate 生成下一批切片写进 §0.3，**先写表再开工**，
   并同步更新顶部当前阶段表。
5. **提交并推送**：按逻辑单元 commit，`git push origin main`。**没 push 就没交接。**

最终回复用这六行，不写长篇叙事：

```text
完成：<E 队列序号 / task ID>
身份：<commit / protocol hash / bundle ID>
验证：<实际命令与 PASS/FAIL>
结果：<只引用 docs/results/PHASE_*.md 位置>
状态：done | wip | blocked；原因
下一步：<E 队列里下一个 todo；是否需要 GPU / 多种子 / 作者授权>
```

长 GPU 任务启动前，仍须按 §5 向作者展示完整命令、cwd 与预期产物；5090 每次单独取得授权。

---

## 附录 · 已移出仓库（2026-09-18）

2026-09-07 → 09-18 的**过程记录**——历次队列、已裁定的决策菜单与推导、三章在跑时的状态、
A4.3 的收口清单、逐轮做了什么——共 1,166 行，已按项目惯例移出仓库：

**`../ekg-backup-20260727/handoff_appendix_20260918/HANDOFF_appendix.md`**

移出理由与 [`ARCHIVE_INDEX.md`](ARCHIVE_INDEX.md) 一致：**旧口径留在仓内会污染检索面与
agent 上下文**，而这部分本来就标着「新窗口不需要读」。git 历史里最后一次含正文的提交可用
`git log -S"（历史）0.3.1" -- docs/HANDOFF.md` 定位。

**结论没有丢**——移出前逐条核过，仍然适用的都已上提：

| 原在附录的东西 | 现在在哪 |
|---|---|
| 作者 2026-09-13「先验能先验的」方针（标着**仍适用**） | **§E.3a**（本文内） |
| 2026-09-15 / 09-16 两批作者裁决的**结论与剩余约束** | §0.5 的表 |
| 三章跑完的终态与归因 | §0.4 + `results/PHASE_A.md` · `PHASE_C.md` · `PHASE_D.md` |
| 历次队列 E.1 / E.2 / E.2a / E.2b | 已被 `EXPERIMENT_PLAN.md` §4 主表取代（§E.0 第 1 条已改指向它） |
| 任务 A / B / C / D 的执行记录 | `results/PHASE_P1.md` · `PHASE_R1.md` |
| A4.3 收口清单、Ch4 设计自审、四项决策菜单的推导 | 移出件里；**只在要查「当时为什么这么判」时去翻** |
